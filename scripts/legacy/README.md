# scripts/legacy — 非构建必需的开发期脚本

这些 Python 脚本不参与任何编译/打包流程(构建入口是
`installer/build_cs.ps1`),仅用于历史开发期验证,且部分依赖
外部克隆的 Python 参考实现(环境变量 `ASTBOX_REF`)。
保留备查;可安全删除。

# scripts/legacy — development-era tools (NOT part of the build)

None of these Python scripts take part in compiling or packaging.
The build entry point is `installer/build_cs.ps1`. Some require an
external clone of the retired Python reference (`ASTBOX_REF`).
Kept for reference only; safe to delete.
